/*class Icon {
    constructor(url, iconWidth, iconHeight){
        this.iconUrl = url;
        this.iconWidth = iconWidth;
        this.iconHeight = iconHeight;
    }

    createNewIcon(){
        let icone = L.icon({
            iconUrl: this.url,
            iconSize: [this.iconWidth, this.iconHeight],
            //iconAnchor: [25, 50]
        });
    }
};
*/


    